{
  "name": "brenden-church-web",
  "version": "1.0.0",
  "description": "A personal website, for me.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && cp -R src/* dist/ && cp -R node_modules/bootstrap/dist dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "express": "^4.19.2"
  }
}
